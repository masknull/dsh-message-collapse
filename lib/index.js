// dsh-message-collapse — host half (minimal).
// All behavior lives in the browser half (./client). This host plugin exists
// so the bundle row resolves; it contributes nothing to the node side.
export const name = 'dsh-message-collapse'
export const inject = []
export function apply() {}
